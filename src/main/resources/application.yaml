spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/BLOG
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  thymeleaf:
    mode: LEGACYHTML5

